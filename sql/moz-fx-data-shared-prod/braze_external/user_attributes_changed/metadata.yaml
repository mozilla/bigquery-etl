friendly_name: User Attributes Changed

description: >
  This table syncs to Braze and updates with payloads based on records
  that have changed since the last sync.

  https://www.braze.com/docs/user_guide/data_and_analytics/cloud_ingestion/overview/#example-first-time-sync-and-subsequent-updates

labels:
  incremental: true

owners:
  - cbeck@mozilla.com
scheduling:
  dag_name: bqetl_braze
  date_partition_parameter:

bigquery:
  time_partitioning:
    type: day
    field:
    require_partition_filter: false
  clustering:
    fields:
