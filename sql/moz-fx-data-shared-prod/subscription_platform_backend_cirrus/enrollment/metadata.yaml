friendly_name: Historical Pings for `subscription-platform-backend-cirrus/enrollment`
description: |-
  A historical view of pings sent for the
  `subscription-platform-backend-cirrus/enrollment`
  document type.

  This view is guaranteed to contain only complete days
  (per `submission_timestamp`)
  and to contain only one row per distinct `document_id` within a given date.

  Clustering fields: `normalized_channel`, `sample_id`
owners: []
labels: {}
workgroup_access:
- role: roles/bigquery.dataViewer
  members:
  - workgroup:mozilla-confidential/data-viewers
references:
  view.sql:
  - moz-fx-data-shared-prod.subscription_platform_backend_cirrus_stable.enrollment_v1
require_column_descriptions: false
