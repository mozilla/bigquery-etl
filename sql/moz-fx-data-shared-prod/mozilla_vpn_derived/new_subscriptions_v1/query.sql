SELECT
  customer_id,
  subscription_id,
  plan_id,
  customer_start_date,
  subscription_start_date,
  fxa_uid,
  country,
  country_name,
  user_registration_date,
  referrer,
  utm_medium,
  utm_source,
  utm_content,
  utm_campaign,
  attribution_category,
  coarse_attribution_category,
  provider,
  plan_amount,
  billing_scheme,
  plan_currency,
  plan_interval,
  product_id,
  pricing_plan,
  product_name,
  normalized_medium,
  normalized_source,
  normalized_campaign,
  normalized_content,
  normalized_acquisition_channel,
  DATE(subscription_start_date) AS `date`,
  website_channel_group,
FROM
  all_subscriptions_v1
WHERE
  (DATE(subscription_start_date) BETWEEN "2020-07-01" AND CURRENT_DATE - 1)
