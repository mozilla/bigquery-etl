friendly_name: Historical Pings for `contextual-services/quicksuggest-block`
description: |-
  A historical view of pings sent for the
  `contextual-services/quicksuggest-block`
  document type.

  This view is guaranteed to contain only complete days
  (per `submission_timestamp`)
  and to contain only one row per distinct `document_id` within a given date.

  Clustering fields: `normalized_channel`, `sample_id`
owners: []
labels: {}
bigquery: null
workgroup_access:
- role: roles/bigquery.dataViewer
  members:
  - workgroup:contextual-services
references:
  view.sql:
  - moz-fx-data-shared-prod.contextual_services_stable.quicksuggest_block_v1
deprecated: false
