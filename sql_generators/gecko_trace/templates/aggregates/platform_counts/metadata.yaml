---
friendly_name: Gecko Trace Platform Counts
description: |-
  Daily trace pattern counts per build, OS and architecture across all Firefox applications.

  Each row has one (app_id, submission_date, trace_signature, platform) and its count,
  with the stable_trace_id of the pattern. Use it to see which platforms a trace
  pattern occurs on.

owners:
  - jjalkanen@mozilla.com
