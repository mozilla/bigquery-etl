# Missing Metadata Result File
# Generated by schema-enricher
# Contains two categories:
#   1. missing_columns — descriptions derived from upstream source schema, query context, or derived from application context
#      (not found in any base schema — live or local)
#   2. local_only_base_schema_columns — descriptions sourced from LOCAL-ONLY base schema files
#      (files that exist locally but have NOT been merged to GitHub main)
#
# Review and take appropriate action for each entry.

table: "moz-fx-data-shared-prod.firefox_desktop_derived.newtab_content_items_daily_v1"
generated_date: "2026-04-23"

# Section 1: Columns whose descriptions came from upstream source schema, query context, or application context
# (not found in any base schema — live or local)
missing_columns:
  - name: newtab_content_surface_id
    type: STRING
    mode: NULLABLE
    inferred_description: >-
      The surface identifier for the newtab content ping.
    inference_basis: upstream source schema (firefox_desktop_derived.newtab_content_reported_content_v1/schema.yaml)
    recommended_target: app_newtab.yaml
    recommendation_reason: >-
      This field identifies the newtab content surface and is used across multiple newtab
      derived tables (e.g., newtab_content_reported_content_v1, newtab_content_items_daily_v1).
      It belongs in app_newtab.yaml as a product-scoped newtab field.

  - name: corpus_item_id
    type: STRING
    mode: NULLABLE
    inferred_description: >-
      A content identifier. For organic Newtab recommendations it is an opaque id produced
      by Newtab's recommendation systems that corresponds uniquely to the URL. This is the
      replacement for tile_id and scheduled_corpus_item_id.
    inference_basis: upstream source schema (firefox_desktop_derived.newtab_items_daily_v1/schema.yaml)
    recommended_target: app_newtab.yaml
    recommendation_reason: >-
      This field is a core Newtab content identifier used across multiple newtab derived
      tables. It belongs in app_newtab.yaml as a product-scoped newtab field.

  - name: newtab_content_ping_version
    type: INTEGER
    mode: NULLABLE
    inferred_description: >-
      The version of the newtab_content ping schema used to send the event. Used to
      distinguish events originating from the dedicated Newtab Content ping versus the
      legacy newtab ping.
    inference_basis: >-
      query context — sourced from metrics.quantity.newtab_content_ping_version; used as
      IS NOT NULL filter to identify events from the Newtab Content ping
    recommended_target: app_newtab.yaml
    recommendation_reason: >-
      This field is specific to the Newtab Content ping versioning scheme and is used
      across newtab content derived tables. It belongs in app_newtab.yaml.

# Summary
# - Columns recommended for global.yaml: []
# - Columns recommended for app_newtab.yaml: [newtab_content_surface_id, corpus_item_id, newtab_content_ping_version]
# - Columns from local-only base schemas (need GitHub merge): []
