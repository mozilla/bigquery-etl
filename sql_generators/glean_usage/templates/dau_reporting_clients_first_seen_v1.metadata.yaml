{{ header_yaml }}
friendly_name: DAU Reporting Clients First Seen
description: |-
  A representation of when we saw each `client_id`
  for the first time based on the dau_reporting ping.

owners:
  - kik@mozilla.com
labels:
  incremental: true
  schedule: daily
scheduling:
  dag_name: bqetl_glean_usage
  task_group: {{ app_name }}
bigquery:
  time_partitioning:
    type: day
    field: submission_date
    require_partition_filter: true
