friendly_name: Newtab Interactions
description: |-
  Interactions on the newtab page, roughly one row per newtab "visit"
  (identified by the visit id).
owners:
  - anicholson@mozilla.com
labels:
  application: firefox
  schedule: daily
#TODO scheduling:
bigquery:
  time_partitioning:
    field: submission_date
    type: day
    require_partition_filter: true
  clustering:
    fields:
      - normalized_channel
      - sample_id
references: {}
