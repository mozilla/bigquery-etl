---
friendly_name: Non-Prod Stripe Subscriptions
description: Non-Prod Stripe Subscriptions as of UTC midnight.
owners:
  - dthorn@mozilla.com
labels:
  application: stripe
  schedule: daily
scheduling:
  dag_name: bqetl_subplat
  # destination is the whole table, not a single partition,
  # so don't use date_partition_parameter
  date_partition_parameter:

# Generated by bigquery_etl.dependency
references:
  query.sql:
  - stripe_external.nonprod_subscriptions_v1
