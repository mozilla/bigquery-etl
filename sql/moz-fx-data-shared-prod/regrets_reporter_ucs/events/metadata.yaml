friendly_name: Historical Pings for `regrets-reporter-ucs/events`
description: |-
  A historical view of pings sent for the
  `regrets-reporter-ucs/events`
  document type.

  This view is guaranteed to contain only complete days
  (per `submission_timestamp`)
  and to contain only one row per distinct `document_id` within a given date.

  Clustering fields: `normalized_channel`, `sample_id`
owners: []
labels: {}
bigquery: null
workgroup_access:
- role: roles/bigquery.dataViewer
  members:
  - workgroup:regrets-reporter
references:
  view.sql:
  - moz-fx-data-shared-prod.regrets_reporter_ucs_stable.events_v1
deprecated: false
