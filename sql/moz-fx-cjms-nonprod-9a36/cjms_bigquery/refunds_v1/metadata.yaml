friendly_name: Stripe refunds on CJ subscriptions
description: Stripe refunds on CJ subscriptions
owners:
  - srose@mozilla.com
labels:
  application: cjms
  schedule: hourly
scheduling:
  dag_name: bqetl_cjms_nonprod
  query_project: moz-fx-data-shared-prod
  # destination is the whole table, not a single partition,
  # so don't use date_partition_parameter
  date_partition_parameter: null

# Generated by bigquery_etl.dependency
references:
  query.sql:
  - EXTERNAL_QUERY('moz-fx-cjms-nonprod-9a36.us.cjms-sql', 'SELECT subscription_id
    FROM subscriptions')
  - moz-fx-data-shared-prod.stripe_external.nonprod_charge_v1
  - moz-fx-data-shared-prod.stripe_external.nonprod_invoice_v1
  - moz-fx-data-shared-prod.stripe_external.nonprod_refund_v1
