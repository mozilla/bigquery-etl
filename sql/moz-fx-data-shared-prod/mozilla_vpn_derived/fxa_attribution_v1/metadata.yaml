friendly_name: Mozilla VPN FxA Attribution
description: >
  Attribution for FxA flow ids for Mozilla VPN, and associated FxA user ids
owners:
  - dthorn@mozilla.com
labels:
  application: mozilla_vpn
  schedule: daily
scheduling:
  dag_name: bqetl_subplat
  # destination is the whole table, not a single partition,
  # so don't use date_partition_parameter
  date_partition_parameter: null
  parameters:
    - "date:DATE:{{ds}}"

# Generated by bigquery_etl.dependency
references:
  query.sql:
  - fxa_attribution_v1
  - moz-fx-fxa-prod-0712.fxa_prod_logs.stdout_20*
  - mozdata.firefox_accounts.fxa_content_auth_events
