friendly_name: Historical Pings for `mlhackweek-search/deletion-request`
description: |-
  A historical view of pings sent for the
  `mlhackweek-search/deletion-request`
  document type.

  This view is guaranteed to contain only complete days
  (per `submission_timestamp`)
  and to contain only one row per distinct `document_id` within a given date.

  Clustering fields: `normalized_channel`, `sample_id`
owners: []
labels: {}
bigquery: null
workgroup_access:
- role: roles/bigquery.dataViewer
  members:
  - workgroup:mlhackweek-search
references:
  view.sql:
  - moz-fx-data-shared-prod.mlhackweek_search_stable.deletion_request_v1
deprecated: false
