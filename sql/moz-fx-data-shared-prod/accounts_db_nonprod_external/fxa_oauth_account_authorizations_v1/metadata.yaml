friendly_name: accountAuthorizations table from nonprod (stage) fxa_oauth database
description: |
  A mirror of the `accountAuthorizations` table from the nonprod (stage) `fxa_oauth` CloudSQL database, updated daily to match the current state of the table.
  See https://mozilla.github.io/ecosystem-platform/reference/database-structure#database-fxa_oauth
owners:
- wclouser@mozilla.com
labels:
  application: accounts_backend
  schedule: daily
  dag: bqetl_accounts_db_daily
  owner1: wclouser
scheduling:
  dag_name: bqetl_accounts_db_daily
  date_partition_parameter: null
workgroup_access:
- role: roles/bigquery.dataViewer
  members:
  - workgroup:accounts-confidential/data-viewers
references:
  query.sql:
  - EXTERNAL_QUERY('moz-fx-fxa-nonprod.us.fxa-oauth-nonprod-stage-fxa-oauth', 'SELECT\n         uid,\n         scope,\n         service,\n         clientId,\n         firstAuthorizedTosAt,\n         lastAuthorizedTosAt\n       FROM\n         fxa_oauth.accountAuthorizations\n    ')
