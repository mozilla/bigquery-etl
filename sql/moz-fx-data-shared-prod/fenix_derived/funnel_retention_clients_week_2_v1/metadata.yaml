friendly_name: (DEPRECATED) User Retention - Firefox Android (Week 2)
description: |
  DEPRECATED, please use `retention_clients` or `retention` instead.

  Derived dataset for Firefox Android retention analysis. Contains metrics that can be calculated
  2 weeks (13 days) after a user was first seen.
owners:
- kik@mozilla.com
labels:
  schedule: daily
  incremental: true
  table_type: client_level
# deprecated and replaced by `retention_clients`
# therefore descheduled on 2025-07-03
# scheduling:
#   dag_name: bqetl_analytics_tables
#   depends_on_past: false
bigquery:
  time_partitioning:
    type: day
    field: submission_date
    require_partition_filter: false
    expiration_days: 775
  clustering:
    fields:
    - sample_id
    - first_seen_date
deprecated: true
