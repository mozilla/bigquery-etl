friendly_name: Guardian Apple Events
description: >
  Guardian Apple IAP Subscriptions reformatted to match Subscription Platform's
  Firestore database for Apple IAP Events after parsing
owners:
  - srose@mozilla.com
labels:
  application: mozilla_vpn
  schedule: daily
scheduling:
  dag_name: bqetl_subplat
  # destination is the whole table, not a single partition,
  # so don't use date_partition_parameter
  date_partition_parameter: null

# Generated by bigquery_etl.dependency
references:
  query.sql:
  - moz-fx-data-shared-prod.mozilla_vpn_external.subscriptions_20230117
  - moz-fx-data-shared-prod.mozilla_vpn_external.users_v1
