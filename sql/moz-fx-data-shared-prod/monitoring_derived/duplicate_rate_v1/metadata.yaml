---
friendly_name: Duplicate Rate
description: >
  Generates a list of distinct and non-distinct document counts per live table.
owners:
  - jklukas@mozilla.com
labels:
  schedule: daily
  incremental: true
scheduling:
  dag_name: bqetl_monitoring
  arguments: ["--date", "{{ ds }}"]
  referenced_tables:
    - ['moz-fx-data-shared-prod', '*_live', '*']
