friendly_name: Structured Pipeline Family Errors
description: |-
  A view on top of the `payload_bytes_error.structured` table that includes all
  fields except the payload.

  This view is useful for overall monitoring of pipeline errors.

  Clustering fields: `submission_timestamp`
owners: []
labels:
  authorized: true
bigquery: null
workgroup_access:
- role: roles/bigquery.dataViewer
  members:
  - workgroup:mozilla-confidential
references:
  view.sql:
  - moz-fx-data-shared-prod.payload_bytes_error.structured
deprecated: false
