friendly_name: Historical Pings for `telemetry/first-shutdown-remainder`
description: |-
  A historical view of pings sent for the
  `telemetry/first-shutdown-remainder`
  document type.

  This view is guaranteed to contain only complete days
  (per `submission_timestamp`)
  and to contain only one row per distinct `document_id` within a given date.

  Clustering fields: `normalized_channel`, `sample_id`
owners: []
labels: {}
bigquery: null
workgroup_access:
- role: roles/bigquery.dataViewer
  members:
  - workgroup:dataops-managed/taar
  - workgroup:mozilla-confidential
references:
  view.sql:
  - moz-fx-data-shared-prod.telemetry_stable.first_shutdown_remainder_v4
deprecated: false
