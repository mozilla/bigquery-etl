---
friendly_name: Non-Prod Stripe Promotion Codes
description: Non-Prod Stripe Promotion Codes as of UTC midnight
owners:
  - dthorn@mozilla.com
labels:
  application: stripe
  schedule: daily
scheduling:
  dag_name: bqetl_subplat
  # destination is the whole table, not a single partition,
  # so don't use date_partition_parameter
  date_partition_parameter:
  parameters:
    - "date:DATE:{{ds}}"

# Generated by bigquery_etl.dependency
references:
  init.sql:
  - nonprod_events_v1
  - nonprod_initial_promotion_codes_v1
  query.sql:
  - nonprod_events_v1
  - nonprod_promotion_codes_v1
