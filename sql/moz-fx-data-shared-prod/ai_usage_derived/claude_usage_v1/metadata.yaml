friendly_name: Claude API Usage
description: |-
  Daily usage data from the Anthropic Admin API, grouped by model and API key.
  Includes token counts (input, output, cached) and web search requests.
owners:
- mcastelluccio@mozilla.com
labels:
  schedule: daily
  incremental: true
bigquery:
  time_partitioning:
    type: day
    field: date
    require_partition_filter: false
scheduling:
  dag_name: bqetl_llm_monitoring
  arguments:
  - --date={{ds}}
  secrets:
  - deploy_target: ANTHROPIC_ADMIN_API_KEY
    key: bqetl_llm_monitoring__anthropic_admin_api_key
