---
friendly_name: TLS Error Reports Pipeline Family Errors
description: |-
  A view on top of the `payload_bytes_error.tls_error_reports` table that includes all
  fields except the payload.

  This view is useful for overall monitoring of pipeline errors.

  Clustering fields: `submission_timestamp`
labels:
  authorized: true
workgroup_access:
  - role: roles/bigquery.dataViewer
    members:
      - workgroup:mozilla-confidential
