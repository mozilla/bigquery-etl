friendly_name: Profile / Client - {{ friendly_name }}
description: |-
  # TODO

owners:
  - mozilla/kpi_table_reviewers
  - kik@mozilla.com
labels:
  schedule: daily
  incremental: true
# will be commented out in a follow up PR which will add a DAG
# scheduling:
#   dag_name:  # TODO: think about which DAG this should run in.
#   depends_on_past: false
#   task_group: {{ app_name }}
#   date_partition_parameter: metric_date
#   parameters:
#     - submission_date:DATE:{{ds}}
bigquery:
  time_partitioning:
    type: day
    field: first_seen_date
    require_partition_filter: false
  clustering:
    fields:
      - normalized_channel
      - country
