friendly_name: Mozilla VPN Waitlist Funnel
description: >
  A count of the number of users that passed through each stage of the Mozilla VPN waitlist funnel.
owners:
  - dthorn@mozilla.com
labels:
  application: stripe
  schedule: daily
scheduling:
  dag_name: bqetl_mozilla_vpn
  # destination is the whole table, not a single partition, so don't use date_partition_parameter
  date_partition_parameter: null
  referenced_tables: [['moz-fx-data-shared-prod', 'mozilla_vpn_external','users_v1']]
