---
description: |
  Convert geoip lookup fields to a struct, replacing NULLs with "??".
  This allows for better joins on those fields, but needs to be changed back to NULL at the end of the query.
friendly_name: Geo Struct Set Defaults
