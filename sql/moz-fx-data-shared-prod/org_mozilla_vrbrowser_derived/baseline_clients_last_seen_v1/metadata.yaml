---
# Generated via bigquery_etl.glean_usage

friendly_name: Baseline Clients Last Seen
description: |-
  Captures activity history of each client in 28-day
  windows for each submission date based on baseline pings.

  Clustering fields: `normalized_channel`, `sample_id`

  See also: `baseline_clients_daily`
owners:
  - jklukas@mozilla.com
bigquery:
  time_partitioning:
    field: submission_date
    type: day
    require_partition_filter: true
  clustering:
    fields:
    - normalized_channel
    - sample_id
schema:
  derived_from:
  - table: ['moz-fx-data-shared-prod', 'org_mozilla_vrbrowser_derived', 'baseline_clients_daily_v1']

# Generated by bigquery_etl.dependency
references:
  init.sql:
  - org_mozilla_vrbrowser_derived.baseline_clients_daily_v1
  query.sql:
  - org_mozilla_vrbrowser_derived.baseline_clients_daily_v1
  - org_mozilla_vrbrowser_derived.baseline_clients_last_seen_v1
