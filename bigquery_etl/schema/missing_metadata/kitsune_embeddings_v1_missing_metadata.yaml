# Missing Metadata Result File
# Generated by schema-enricher
# Contains columns whose descriptions were derived from query context or application context
# (not found in any base schema — live or local)

table: "moz-fx-data-shared-prod.customer_experience_derived.kitsune_embeddings_v1"
generated_date: "2026-04-02"

# Section 1: Columns whose descriptions came from query context or application context
# (not found in any base schema — live or local)
missing_columns:
  - name: "creation_date"
    type: "TIMESTAMP"
    mode: "NULLABLE"
    inferred_description: >-
      Timestamp when the Kitsune support question was originally created.
    inference_basis: "query context — aliased from created_date in kitsune_questions source"
    recommended_target: "customer_experience_derived.yaml"
    recommendation_reason: >-
      Column is specific to the customer experience domain and Kitsune support data.

  - name: "title"
    type: "STRING"
    mode: "NULLABLE"
    inferred_description: >-
      Title of the support question as submitted by the user on Kitsune (SUMO).
    inference_basis: "query context — pass-through from kitsune_questions source"
    recommended_target: "customer_experience_derived.yaml"
    recommendation_reason: >-
      Column is specific to Kitsune support question content.

  - name: "content"
    type: "STRING"
    mode: "NULLABLE"
    inferred_description: >-
      Full text body of the support question as submitted by the user on Kitsune (SUMO).
    inference_basis: "query context — aliased from question_content in kitsune_questions source"
    recommended_target: "customer_experience_derived.yaml"
    recommendation_reason: >-
      Column is specific to Kitsune support question content.

  - name: "product"
    type: "STRING"
    mode: "NULLABLE"
    inferred_description: >-
      Mozilla product that the Kitsune support question is associated with (e.g., Firefox, Thunderbird).
    inference_basis: "query context — pass-through from kitsune_questions source"
    recommended_target: "customer_experience_derived.yaml"
    recommendation_reason: >-
      In this context product refers specifically to Mozilla products in the Kitsune support domain, not the general product field.

  - name: "topic"
    type: "STRING"
    mode: "NULLABLE"
    inferred_description: >-
      Topic category assigned to the support question on Kitsune (SUMO).
    inference_basis: "query context — pass-through from kitsune_questions source"
    recommended_target: "customer_experience_derived.yaml"
    recommendation_reason: >-
      Column is specific to Kitsune support question categorization.

  - name: "type"
    type: "STRING"
    mode: "NULLABLE"
    inferred_description: >-
      Type of the Kitsune content record; currently always set to "question".
    inference_basis: "query context — hardcoded literal 'question' in SELECT"
    recommended_target: "customer_experience_derived.yaml"
    recommendation_reason: >-
      Column is specific to Kitsune content type classification.

  - name: "is_solved"
    type: "BOOLEAN"
    mode: "NULLABLE"
    inferred_description: >-
      Whether the support question has been marked as solved on Kitsune (SUMO).
    inference_basis: "query context — pass-through boolean from kitsune_questions source"
    recommended_target: "customer_experience_derived.yaml"
    recommendation_reason: >-
      Column is specific to Kitsune support question resolution status.

  - name: "votes"
    type: "INTEGER"
    mode: "NULLABLE"
    inferred_description: >-
      Number of votes the support question has received from Kitsune users.
    inference_basis: "query context — aliased from num_votes in kitsune_questions source"
    recommended_target: "customer_experience_derived.yaml"
    recommendation_reason: >-
      Column is specific to Kitsune support question engagement.

  - name: "summary_generated"
    type: "STRING"
    mode: "NULLABLE"
    inferred_description: >-
      AI-generated summary of the support question, limited to a maximum of 20 words, produced by the Gemini model.
    inference_basis: "query context — output from AI.GENERATE_TABLE with Gemini model"
    recommended_target: "customer_experience_derived.yaml"
    recommendation_reason: >-
      AI-generated field specific to the customer experience analysis pipeline.

  - name: "category_generated"
    type: "STRING"
    mode: "NULLABLE"
    inferred_description: >-
      AI-generated classification label for the support question, consisting of one to two words, produced by the Gemini model.
    inference_basis: "query context — output from AI.GENERATE_TABLE with Gemini model"
    recommended_target: "customer_experience_derived.yaml"
    recommendation_reason: >-
      AI-generated field specific to the customer experience analysis pipeline.

  - name: "language_generated"
    type: "STRING"
    mode: "NULLABLE"
    inferred_description: >-
      AI-detected ISO language code (e.g., en, es, fr) for the support question content, produced by the Gemini model.
    inference_basis: "query context — output from AI.GENERATE_TABLE with Gemini model"
    recommended_target: "customer_experience_derived.yaml"
    recommendation_reason: >-
      AI-generated field specific to the customer experience analysis pipeline.

  - name: "entities_generated"
    type: "STRING"
    mode: "REPEATED"
    inferred_description: >-
      Array of up to five AI-extracted normalized entities from the support question, produced by the Gemini model.
    inference_basis: "query context — output from AI.GENERATE_TABLE with Gemini model"
    recommended_target: "customer_experience_derived.yaml"
    recommendation_reason: >-
      AI-generated field specific to the customer experience analysis pipeline.

  - name: "topics_generated"
    type: "STRING"
    mode: "REPEATED"
    inferred_description: >-
      Array of up to three AI-generated normalized topic labels for the support question, produced by the Gemini model.
    inference_basis: "query context — output from AI.GENERATE_TABLE with Gemini model"
    recommended_target: "customer_experience_derived.yaml"
    recommendation_reason: >-
      AI-generated field specific to the customer experience analysis pipeline.

  - name: "sentiment_score"
    type: "FLOAT"
    mode: "NULLABLE"
    inferred_description: >-
      AI-generated sentiment score ranging from -1.0 (very negative) to 1.0 (very positive), with 0 as neutral, produced by the Gemini model.
    inference_basis: "query context — output from AI.GENERATE_TABLE with Gemini model"
    recommended_target: "customer_experience_derived.yaml"
    recommendation_reason: >-
      AI-generated field specific to the customer experience analysis pipeline.

  - name: "embedding"
    type: "FLOAT"
    mode: "REPEATED"
    inferred_description: >-
      Dense vector embedding of the concatenated title and content, generated by the Gemini embedding model (gemini-embedding-001).
    inference_basis: "query context — output from AI.EMBED with gemini-embedding-001 endpoint"
    recommended_target: "customer_experience_derived.yaml"
    recommendation_reason: >-
      AI-generated embedding field specific to the customer experience analysis pipeline.

  - name: "recency_score"
    type: "FLOAT"
    mode: "NULLABLE"
    inferred_description: >-
      Exponential decay score measuring content freshness, computed as exp(-age_in_days / 7) where 7 days is the half-relevance window.
    inference_basis: "query context — computed expression EXP(-DATE_DIFF(...) / 7)"
    recommended_target: "customer_experience_derived.yaml"
    recommendation_reason: >-
      Computed metric specific to the customer experience content relevance model.

  - name: "metadata"
    type: "RECORD"
    mode: "NULLABLE"
    inferred_description: >-
      Technical metadata about the AI analysis pipeline, including model versions, quality scores, and processing status.
    inference_basis: "query context — STRUCT constructed from AI.GENERATE_TABLE output with added status field"
    recommended_target: "customer_experience_derived.yaml"
    recommendation_reason: >-
      Pipeline metadata record specific to the customer experience AI analysis.

# Summary
# - Columns recommended for global.yaml: []
# - Columns recommended for customer_experience_derived.yaml: [creation_date, title, content, product, topic, type, is_solved, votes, summary_generated, category_generated, language_generated, entities_generated, topics_generated, sentiment_score, embedding, recency_score, metadata]
