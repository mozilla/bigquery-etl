
# Generated by bigquery_etl.dependency
references:
  view.sql:
  - moz-fx-data-shared-prod.telemetry_derived.experiments_v0 /* This table was called
    experiments_v1 in the moz-fx-data-derived-datasets */ /* project, but we renamed
    to v0 when copying to shared-prod so that we */ /* could maintain this query that
    unions the old and new structures. */
  - moz-fx-data-shared-prod.telemetry_derived.experiments_v1
