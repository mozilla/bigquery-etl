friendly_name: Delete Users

description: >
  This table syncs to Braze and deletes users for records added
  since the last sync.

  https://www.braze.com/docs/user_guide/data_and_analytics/cloud_ingestion/delete_users/

labels:
  incremental: true

owners:
  - cbeck@mozilla.com
scheduling:
  dag_name: bqetl_braze
  date_partition_parameter:

bigquery:
  time_partitioning:
    type: day
    field:
    require_partition_filter: false
  clustering:
    fields:
