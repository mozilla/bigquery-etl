friendly_name: Detail Export Raw
description: >
  Import detail export iProspect CSV data from
  moz-fx-data-marketing-prod-iprospect storage bucket.

  Each partition represents CSV data that was uploaded
  for that date.
labels:
  incremental: true
owners:
  - ascholtz@mozilla.com
scheduling:
  dag_name: bqetl_iprospect
  arguments: ["--date", "{{ ds }}"]
